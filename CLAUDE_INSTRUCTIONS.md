# CLAUDE_INSTRUCTIONS.md

このファイルには、Claude Codeに対する人間からの指示や設定を記載します。

## このリポジトリの目的

コードレビュー支援サービスの試運転を目的とした public リポジトリです。**機能はすべて Pull Request として追加してください。** 初期コミットに機能を混ぜないでください。

## 言語設定
- **必ず日本語で応答してください**
- エラーメッセージや技術的な説明も日本語で記述してください
- コード内のコメントは既存のスタイルに従ってください

## プロジェクト固有のルール

### コーディング規約
- 開発方針は @開発方針.md を参照してください
- 新しいコンポーネントを作成する際は、既存のコンポーネントのスタイルに合わせてください
- TypeScriptの型定義は必ず行ってください

### コミットメッセージ

このプロジェクトでは、コミットメッセージを以下の形式で記載してください。

```
type: 日本語で1行 (commited by claude code)
```

#### フォーマット

- **type** は変更の種類を表します（英語小文字）。
- コロン `:` の後に **変更内容を日本語で簡潔に1行** で記載してください。
- 語尾は「〜を追加」「〜を修正」「〜を削除」などの **命令形/体言止め** で統一します。

#### type 一覧

- `feat` : 新機能の追加
- `fix` : バグ修正
- `docs` : ドキュメントの変更
- `style` : コードスタイル（動作に影響しない空白や整形など）
- `refactor` : リファクタリング（機能追加・修正を伴わない）
- `test` : テストコードの追加・修正
- `chore` : ビルドや設定、ツール関連の変更

#### 例

```
feat: プロフィール編集画面を追加 (commited by claude code)
fix: ログイン時にセッションが切れる不具合を修正 (commited by claude code)
refactor: 認証処理を共通モジュールに分離 (commited by claude code)
docs: API使用方法をREADMEに追記 (commited by claude code)
```

#### 補足

- コミットは **1コミット = 1つの論理的な変更** を意識してください。
- どうしても補足が必要な場合は本文（body）を使って記載しても構いません。
- このスタイルは [Conventional Commits](https://www.conventionalcommits.org/) を日本語向けに簡略化したものです。

### テスト
- 新機能追加時は、可能な限りStorybookストーリーを作成してください
- フロントエンドは `npm run lint` と `npm run type-check` でコード品質をチェックしてください
- APIのテストはモックを使わず、テスト用データベースへ接続する統合テストで書いてください

## 開発時の注意事項

### Docker環境での開発
- **フロントエンドのnpmコマンドは必ずDockerコンテナ内で実行してください**
- `docker compose up -d`でコンテナを起動してから作業を開始してください
- `docker compose exec frontend [コマンド]`の形式で実行するか、コンテナに入って作業してください

### ソースコードの自動生成
- 自動生成されている旨のコメントが記載されているファイルは直接編集しないでください。
- 生成の定義は `generator/src/seed/` にあります。定義を変更したら `sampleapp local generate` を実行してください。詳細は @開発方針.md を参照してください。
- テーブル・APIスキーマ・エンドポイント・区分値・エラーコード・画面のいずれを変更した場合も、生成物（Rust / TypeScript / SQL / Vue / OpenAPI / Markdown）が一括で最新化されます。

### セキュリティ
- 認証情報やAPIキーをコードに含めないでください
- public リポジトリのため、個人情報や実在するドメイン・アカウントIDを含めないでください

### パフォーマンス
- コンポーネントの再レンダリングを最小限に抑えてください
- 大きなコンポーネントは適切に分割してください

### アクセシビリティ
- 適切なARIA属性を追加してください
- キーボード操作に対応してください

## よくある質問への対応

### エラー対応
- フロントエンドでエラーが発生した場合は、まず`npm install`を実行してください

### /exec-issueコマンドの確実な実行
`/exec-issue` コマンドを使用する際は以下を必ず守ってください：

1. **TodoWriteツールを最初に使用**
   - コマンド実行開始時に必ずTodoWriteツールでタスクリストを作成
   - 各手順の完了時にステータスを更新

2. **すべての手順を順番に実行**
   - Issue理解 → ブランチ準備 → 実装 → 品質チェック → コミット → PR作成
   - どの手順もスキップしてはいけません

3. **コミット粒度の徹底**
   - 1コミット = 1つの論理的な変更
   - フォーマット修正、機能追加、ヘッダー変更などは別々のコミット

4. **PRの必須要素**
   - `Closes #[issue番号]` をPR本文に記載
   - 適切なタイトルと説明

## CLAUDE.mdのメンテナンス

### 自動更新ルール
仕様や実装とCLAUDE.mdの内容に乖離が発生した場合は、CLAUDE.mdを自動的に更新してください：
