# 開発方針
このドキュメントでは、sample-app の開発方針について説明します。

## このリポジトリについて

コードレビュー支援サービスの試運転を目的とした public リポジトリです。**機能はすべて Pull Request として追加してください。** 初期コミットに機能を混ぜないでください。

## アプリケーション構成

管理画面とメンバー画面は、アプリケーションを分離しています。データベースは1つを共有します。

| アプリケーション | 役割 | ポート |
| --- | --- | --- |
| `apps/frontend` | メンバー画面 | 8080 |
| `apps/api` | メンバーAPI | 3000 |
| `apps/admin_frontend` | 管理画面 | 8081 |
| `apps/admin_api` | 管理API | 3001 |

管理APIとメンバーAPIは別クレート・別プロセスであり、互いに依存しません。認証やセッションもアプリケーションごとに独立します。

生成物の分離方針は次のとおりです（詳細は「コードの自動生成について」を参照）。

- **共通（DBを共有するため）**: テーブル定義・区分値。定義は1つで、生成物を両アプリへ配布する。
- **アプリケーション固有**: エラーコード・エンティティID・APIスキーマ・APIエンドポイント・画面。ターゲットごとに定義し、対応するアプリへ生成する。

## コーディング規約

### 区分値の利用
原則として、アプリケーションで利用する列挙型は区分値として定義します。2値であっても、boolean型を直接利用せず、区分値を利用してください。

### APIエラーの扱い
APIのエラーレスポンスには、HTTPステータスコードに加えて、エラーコードを含めるようにしてください。
また、原則として画面に表示するエラーメッセージは、クライアント側で用意せず、エラーコードから導出するようにしてください。

### スキーマ分離と論理削除
各テーブルは `source`（親）・`public`（生存行）・`garbage`（論理削除された行）の3スキーマで構成します。**論理削除は、対象行を `public` から `garbage` へ移動することで表現します。**

そのため、通常のクエリはスキーマ修飾なしで `public` を参照し（例: `SELECT ... FROM users`）、`deleted_at IS NULL` のような削除フラグによる絞り込みは**書きません**。`public` には生存行しか存在しないためです。

### レイヤードアーキテクチャ

APIサーバーは単一クレートの中を以下の3層に分けています。

- **handler**: プレゼンテーション層（リクエストの受け取りとレスポンスの組み立て）
- **service**: ビジネスロジック層（axum の型に依存しない）
- **repository**: 永続化層（`&PgPool` を受け取り SQL を発行する）

依存方向: `handler → service → repository`（逆方向の依存は禁止）。詳細は `apps/api/README.md` を参照してください。

### コードの自動生成について

以下の情報は `generator/src/seed` フォルダ内のファイルを元に自動生成します。直接変更せず、`sampleapp local generate` コマンドを実行して最新化してください。service 層（`apps/api/src/service/`、`apps/admin_api/src/service/`）は自動生成されないため、手動で実装します。

生成先はアプリケーションごとに分かれます。`generator/src/generators/targets.ts` にターゲット（member / admin）と出力先を定義しています。

- `apps/frontend`（member）、`apps/admin_frontend`（admin）
  - APIリクエスト/レスポンスの型
  - API実行クライアント
  - 区分値（共通定義から両アプリへ配布）
  - エラーコード
  - ページの EntryPoint、ルーター定義
- `apps/api`（member）、`apps/admin_api`（admin）
  - APIリクエスト/レスポンスの型
  - ルーティング、ハンドラのディスパッチ
  - エンティティID
  - 区分値（共通定義から両アプリへ配布）
  - エラーコード
  - テスト用のDB初期化SQL
- `db/sql/foreign_keys`, `db/sql/tables`
  - テーブル定義
  - 外部キー定義
- `docs/open_api/api`（member）、`docs/open_api/admin_api`（admin）
  - OpenAPIスキーマ
- `docs/database`, `docs/constants`
  - ER図、テーブル詳細、区分値一覧
- `docs/screens`（member）、`docs/admin_screens`（admin）
  - 画面遷移図、画面詳細
